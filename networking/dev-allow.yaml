apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dev-allow-internal
  namespace: dev
spec:
  podSelector: {}
  ingress:
  - from:
    - podSelector: {}
  egress:
  - to:
    - podSelector: {}
